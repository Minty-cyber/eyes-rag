services:
  eyes-rag:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: eyes-rag
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - .:/app 
      - /app/.venv 
    restart: unless-stopped
